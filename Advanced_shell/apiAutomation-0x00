#!/bin/bash
API_URL="https://pokeapi.co/api/v2/pokemon/pikachu"
OUTPUT_FILE="data.json"
ERROR_FILE="errors.txt"
if curl -s -f "$API_URL" -o "$OUTPUT_FILE"; then
	exit 0
else
	echo "Error: Failed to retrieve data for Pikachu from $API_URL" >> "$ERROR_FILE"
	exit 1
fi
